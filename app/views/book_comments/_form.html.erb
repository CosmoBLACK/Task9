<%= form_with model: [book, comment], url: book_book_comments_path(book), locals: false do |f| %>
  <%= f.text_area :comment, class: 'form-control comment_textarea' %>
  <%= f.submit "送信" %>
<% end %>